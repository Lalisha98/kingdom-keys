[build]
  command = "npm run build"

  # Folder to publish after build
  publish = "dist"

  # Optional: directory for Netlify Functions (serverless functions)
  functions = "netlify/functions"

# Optional: SPA redirect rule (uncomment if using a SPA like React)
#[[redirects]]
#  from = "/*"
#  to = "/index.html"
#  status = 200

